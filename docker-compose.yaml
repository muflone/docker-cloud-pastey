services:
  app:
    image: cesura/pastey:0.5.1-slim
    user: nobody:nogroup
    networks:
      - default
    env_file:
      - ./app.env
    volumes:
      - data:/app/data

networks:
  default:

volumes:
  data:

